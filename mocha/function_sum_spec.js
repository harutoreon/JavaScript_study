describe("sum", function() {

  it("Does the total value equal 4?", function() {
    assert.equal(sum(3, 1), 4);
  });

  it("Does the total value equal 8?", function() {
    assert.equal(sum(5, 3), 8);
  });

});
